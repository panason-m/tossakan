<Window
    x:Class="Tossakan.Views.AttachmentPreviewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel x:Name="Toolbar" Grid.Row="0" Orientation="Horizontal" Spacing="8"
                    Padding="12,8" Visibility="Collapsed">
            <Button Content="Zoom in" Click="ZoomIn_Click" />
            <Button Content="Zoom out" Click="ZoomOut_Click" />
            <Button Content="Fit to window" Click="ZoomFit_Click" />
        </StackPanel>

        <ScrollViewer x:Name="Scroller" Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="ContentHost" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </ScrollViewer>
    </Grid>
</Window>
